# automacao/.env
# Copie para .env e ajuste os valores
# IMPORTANTE: AUTOMACAO_API_KEY deve ser IGUAL ao AUTOMACAO_API_KEY do .env do Laravel

AUTOMACAO_API_KEY=gere-uma-chave-segura-aqui

# Credenciais FV (só para teste local — diagnostico_proprietario.py)
AUTOMACAO_FV_USUARIO=
AUTOMACAO_FV_SENHA=

AUTOMACAO_DB_PATH=/tmp/automacao_db/jobs.db
AUTOMACAO_SCREENSHOT_DIR=/tmp/automacao_screenshots

# Perfil Chrome persistente — reutiliza a sessão do FV e evita reCAPTCHA
AUTOMACAO_CHROME_PROFILE_DIR=/tmp/automacao_chrome

# reCAPTCHA do login FV (só no primeiro acesso ou quando a sessão expirar).
# Use um dos dois — Capsolver costuma ser mais rápido.
# CAPSOLVER_API_KEY=
# TWOCAPTCHA_API_KEY=
